# stagefy Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-28

## Active Technologies
- Groovy (Jenkins Shared Library, Jenkins Pipeline DSL) + Jenkins Pipeline: Utility Steps Plugin (`readYaml`), `org.jenkinsci.plugins.pipeline.modeldefinition.Utils` (002-parallel-stage-templates)
- YAML files (Jenkins.yml) — read via `readYaml` at pipeline runtime (002-parallel-stage-templates)

- Groovy (Jenkins Shared Library, Pipeline DSL) + Jenkins Pipeline: Utility Steps Plugin (`readYaml`), `org.jenkinsci.plugins.pipeline.modeldefinition.Utils` (001-steps-use-directive)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Groovy (Jenkins Shared Library, Pipeline DSL)

## Code Style

Groovy (Jenkins Shared Library, Pipeline DSL): Follow standard conventions

## Recent Changes
- 002-parallel-stage-templates: Added Groovy (Jenkins Shared Library, Jenkins Pipeline DSL) + Jenkins Pipeline: Utility Steps Plugin (`readYaml`), `org.jenkinsci.plugins.pipeline.modeldefinition.Utils`

- 001-steps-use-directive: Added Groovy (Jenkins Shared Library, Pipeline DSL) + Jenkins Pipeline: Utility Steps Plugin (`readYaml`), `org.jenkinsci.plugins.pipeline.modeldefinition.Utils`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
